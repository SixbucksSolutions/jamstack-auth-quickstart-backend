# "org" ensures this Service is used with the correct Serverless Framework Access Key.
org: firsttracks

# "app" enables Serverless Framework Dashboard features and sharing them with other Services.
app: fafauth-api

# "service" is the name of this project. This will also be added to your AWS resource names.
service: jamstack-auth-quickstart


provider:
  name: aws
  region: af-south-1
  runtime: python3.14

  domain:
    name: api.jamstack-auth.publicntp.net
    certificateArn: arn:aws:acm:af-south-1:293119271420:certificate/3d8d5f07-08e3-46c7-bae6-b9f794b8de8d
    createRoute53Record: false

  httpApi:
    authorizers:
      kindeTokenAuthorizer:
        identitySource: $request.header.Authorization
        issuerUrl: https://sixbuckssolutionsllc.kinde.com
        audience:
          - api.jamstack-auth.publicntp.net

functions:

  ping:
    handler: handler.ping
    memorySize: 128
    events:
      - httpApi:
          path: /api/v001/ping
          method: get
    url:
      cors:
        allowedOrigins:
          - https://jamstack-auth.publicntp.net


  userGet:
    handler: handler.user_get
    memorySize: 128
    events:
      - httpApi:
          path: /api/v001/user
          method: get
          authorizer:
            name: kindeTokenAuthorizer
    url:
      cors:
        allowedOrigins:
          - https://jamstack-auth.publicntp.net
        allowedHeaders:
          - Authorization
        allowCredentials: true
